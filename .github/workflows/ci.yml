# Tests unitaires à chaque push (sans SofaScore).
# Sert aussi à « allumer » l’onglet Actions si seul le cron était présent.

name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
      - name: Install
        run: pip install -r requirements-dev.txt
      - name: Pytest
        run: pytest -q
